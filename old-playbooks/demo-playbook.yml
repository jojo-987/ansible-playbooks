---
- name: Test jenkins
  hosts:
    - 192.168.0.104
  tasks:
    - name: find playbooks
      find:
        path: ~/Desktop/intern/playbooks
        patterns: "*.yml"
      register: allPlaybooks
    - name: debug
      debug:
        msg: "{{ allPlaybooks }}"
